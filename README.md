# How it works

I need to get an outline of how everything is running inside state (horrors of stores with side effects. Dont do this, kids) so this can be extended by anyone. Mod support and allthat

For now just pull it and bun i/bun run dev. I'll document all the sideeffect spaghetty later when i'll allow hooking of mods (lmao i'll need to make enough content first)

# AI disclosure

Due to /r/incremental_games rules i'll need to provide AI disclosure, which i will start with. Ill keep it here so i can copy-paste it

Project is uploaded on GitHub, which uses AI. 

Words that i dont know english equivalent for were translated using Google Translate, which is based on [Transformer](https://en.wikipedia.org/wiki/Transformer_(deep_learning)) algorythm which is [basis of all modern LLMS](https://en.wikipedia.org/wiki/Attention_Is_All_You_Need)

Code was written in TypeScript, which allows LLM-generated commits and uses co-pilot for auto-reviews

Frontend is done via Svelte, which allows LLM-generated commits and uses co-pilot reviews, it is backed by Vercel, which uses AI in its workflows and serves AI to users

I used Vite for bundling/local testing, which has AI-based [pull](https://github.com/vitejs/vite/pull/21734) [requests](https://github.com/vitejs/vite/pull/21116) [merged](https://github.com/vitejs/vite/pull/20522)

Styling is done via TailwindCSS, which has AI-related approved [pull](https://github.com/tailwindlabs/tailwindcss/pull/18947) [requests](https://github.com/tailwindlabs/tailwindcss/pull/19131) in it.

Fastq — one of dependencies in chain — also has AI-generated [pull](https://github.com/mcollina/fastq/pull/98) [requests](https://github.com/mcollina/fastq/pull/101) in it. 

This post is shared on Reddit, which uses AI-based search and suggestions via AI analysis of contents. Replying, reading or interacting in any way with this post will inadvertently (this word was checked in google translate, which uses AI/transformer algo) make you use AI. Please stop using reddit immediately if you find that unacceptable.

This promo text was written in .md for ease of use by me. Ai agent workflows frequently use markdown files

Code was written using VSCode and NeoVim, both of which are accepting and using AI. My current MacOS version is 26.4, which has AI-related features. 

No AI was used in making of the game, including CI/CD workflows being written manually (it sucks, CI to pages is pain, but imma not paying for hosting to post SPA game). 

I've used a lot of AI to improve my coding overall, including consulting on different issues arising during my work, meaning my professional skills are related to AI. 

Some of the related tooling is made via AI because i have better things to do than making custom VSCode node graph plugin that parses JS AST to show me connected nodes manually that only i will use. Deal with it. You'll never see it anyways.

Migrator migrate.ts is also slopped. Deal with it. Thanks to this im less bothered by having to write migrations when i re-align some content that i didnt like — naming location after my favourite fetch land wasnt the best idea.

I use AI to practice English and Japanese, conversing with me means supporting AI workflows and interacting with slop-based language skills.

Commit descriptions are auto-generated by github, becaue i dont care enough to keep proper changelog